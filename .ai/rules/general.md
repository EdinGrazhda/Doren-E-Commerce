---
paths:
  - '**'
---

# General

## Mandatory RTK commands and Graphify refresh
Follow root RTK.md and GRAPHIFY.md. Route all terminal commands through an RTK wrapper or rtk proxy for exact/unsupported commands. After each prompt that changes website files or project instructions, finish checks then run rtk proxy graphify update . from the repository root and verify completion before replying. Skip read-only prompts; normal refreshes must not invoke paid semantic/LLM extraction.
